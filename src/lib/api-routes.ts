// Reference API routes (not included in static export)
// These can be moved into the app directory when switching to server mode
//
// POST /api/contact — Contact form submission
// POST /api/register — Tournament registration
// POST /api/book — Trial class booking
//
// All accept JSON body and return { success: boolean, message: string }